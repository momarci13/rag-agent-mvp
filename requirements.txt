# Hosted LLM + embeddings (OpenRouter, OpenAI-compatible; no local models)
httpx>=0.27.0
fastapi>=0.115.0
uvicorn[standard]>=0.23.0

# Data & stats (CPU, no GPU)
numpy>=1.26.0
pandas>=2.2.0
scipy>=1.13.0
scikit-learn>=1.4.0
statsmodels>=0.14.0

# RAG stack
chromadb>=0.5.0
rank-bm25>=0.2.2
pypdf>=4.2.0
tiktoken>=0.5.0
nltk>=3.8.1

# Market data (free)
yfinance>=0.2.40

# IBKR TWS API (research works without it; required only for order submission)
ibapi>=9.81.1.post1

# Backtesting / portfolio math (pure Python, no C solvers required)
cvxpy>=1.5.0

# Writing pipeline
pydantic>=2.7.0
PyYAML>=6.0.1
jinja2>=3.1.4

# Streaming (SSE for background task progress)
sse-starlette>=1.8.2

# Dev / tests
pytest>=8.0.0
ruff>=0.8.0
